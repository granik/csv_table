name: 'CSV Table'
type: module
description: 'Provides a Block which uses a CSV as source for building a Table whose results can be filtered via an input field'
package: 'Content'
core_version_requirement: ^8.8 || ^9
dependencies:
  - drupal:block